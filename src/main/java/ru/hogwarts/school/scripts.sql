select * from student

SELECT * FROM Student
WHERE age BETWEEN 10 AND 20;

SELECT name FROM Student;

SELECT * FROM Student
WHERE name LIKE '%к%';

SELECT * FROM Student
WHERE age < id;

SELECT * FROM Student
ORDER BY age;